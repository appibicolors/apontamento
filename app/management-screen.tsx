"use client";
import {FormEvent,useState} from "react";
import {createMachine,createOperator,Maquina,Perfil} from "./supabase";
type Props={kind:"machines"|"operators";machines:Maquina[];profiles:Perfil[];token:string;onRefresh:()=>Promise<void>;onClose:()=>void};
export function ManagementScreen({kind,machines,profiles,token,onRefresh,onClose}:Props){const [busy,setBusy]=useState(false);const [message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage("");const f=new FormData(e.currentTarget);try{if(kind==="machines")await createMachine(token,{codigo:String(f.get("codigo")),nome:String(f.get("nome")),setor:String(f.get("setor"))});else await createOperator({nome:String(f.get("nome")),email:String(f.get("email")),password:String(f.get("password"))});e.currentTarget.reset();await onRefresh();setMessage(kind==="machines"?"Máquina cadastrada com sucesso.":"Operador criado. Se a confirmação de e-mail estiver ativa, ele receberá uma mensagem do Supabase.")}catch(error){setMessage(error instanceof Error?error.message:"Não foi possível cadastrar.")}finally{setBusy(false)}}const rows=kind==="machines"?machines:profiles.filter(p=>p.perfil==="operador");return <section className="manage-screen"><header><div><p className="eyebrow">CADASTRO</p><h2>{kind==="machines"?"Máquinas":"Operadores"}</h2></div><button onClick={onClose}>×</button></header><div className="manage-grid"><form onSubmit={submit}><h3>Novo {kind==="machines"?"equipamento":"operador"}</h3>{kind==="machines"?<><label>Código<input name="codigo" required placeholder="Ex.: RAMA 01"/></label><label>Nome<input name="nome" required placeholder="Nome da máquina"/></label><label>Setor<input name="setor" placeholder="Ex.: Acabamento"/></label></>:<><label>Nome<input name="nome" required/></label><label>E-mail<input name="email" type="email" required/></label><label>Senha temporária<input name="password" type="password" minLength={6} required/></label></>}{message&&<p role="status">{message}</p>}<button className="primary" disabled={busy}>{busy?"Cadastrando…":"＋ Cadastrar"}</button></form><div className="manage-list"><h3>Cadastrados ({rows.length})</h3>{rows.length?rows.map(item=><article key={item.id}><span>{"codigo" in item?item.codigo:item.nome.slice(0,2).toUpperCase()}</span><div><b>{item.nome}</b><small>{"codigo" in item?(item.setor||"Sem setor"):"Operador ativo"}</small></div></article>):<p>Nenhum cadastro encontrado.</p>}</div></div></section>}
