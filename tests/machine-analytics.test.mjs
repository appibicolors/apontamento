import assert from "node:assert/strict";import test from "node:test";import {machineSeconds,machineState} from "../app/machine-analytics.ts";
const machine={id:"m1",codigo:"M-1",nome:"Máquina",setor:null};const order={id:"o1",numero_op:"1",cliente:"C",artigo:"A",status:"em_producao"};const operation={id:"x1",codigo:"0001",descricao:"Teste",status:"em_andamento",sequencia:1};
const execution=(point)=>({order,operation,point:{id:Math.random().toString(),operador_id:"u1",maquina_id:"m1",observacao:null,...point}});
test("não multiplica o tempo de um grupo aberto pelas OPs",()=>{const rows=[execution({inicio_em:"2026-09-16T10:00:00Z",termino_em:null,grupo_id:"g1"}),execution({inicio_em:"2026-09-16T10:00:00Z",termino_em:null,grupo_id:"g1"})];assert.equal(machineSeconds(rows,"m1",new Date("2026-09-16T09:00:00Z").getTime(),new Date("2026-09-16T12:00:00Z").getTime(),new Date("2026-09-16T11:00:00Z").getTime()),3600);assert.equal(machineState(rows,machine).state,"running")});
test("soma o rateio final de grupos e reconhece máquina parada",()=>{const rows=[execution({inicio_em:"2026-09-16T10:00:00Z",termino_em:"2026-09-16T11:00:00Z",grupo_id:"g1",duracao_rateada_segundos:1200,motivo_finalizacao:"pausa"}),execution({inicio_em:"2026-09-16T10:00:00Z",termino_em:"2026-09-16T11:00:00Z",grupo_id:"g1",duracao_rateada_segundos:2400,motivo_finalizacao:"pausa"})];assert.equal(machineSeconds(rows,"m1",new Date("2026-09-16T09:00:00Z").getTime(),new Date("2026-09-16T12:00:00Z").getTime()),3600);assert.equal(machineState(rows,machine).state,"stopped")});
